@using EFarming.DTO.QualityModule;
@model IEnumerable<EFarming.DTO.QualityModule.SensoryProfileAssessmentDTO>

@*No se utiliza el pagedList se documeta*@
@*@model IEnumerable<EFarming.DTO.QualityModule.SensoryProfileAssessmentDTO>*@
@*@using PagedList.Mvc;*@
@*<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />*@

@{
    ViewBag.Title = ReportMessage.Quality_Assessments_By_Taster;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section breadcrumb{
    <div class="row wrapper border-bottom white-bg page-heading">
        <div class="col-lg-10">
            <h2 class="page-name">@ReportMessage.Quality_Assessments_By_Taster</h2>
            <ol class="breadcrumb">
                <li>
                    <a href="@Url.Action("Index", "Home")">
                        @Message.Home
                    </a>
                </li>
                <li>
                    <a href="@Url.Action("Index", "Reports")">
                        @ReportMessage.Reports
                    </a>
                </li>
                <li class="active">
                    <strong>@ReportMessage.Quality_Assessments_By_Taster</strong>
                </li>
            </ol>
        </div>
        <div class="col-lg-2">
        </div>
    </div>
}
<div class="wrapper wrapper-content">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5>@ReportMessage.Reports</h5>
                </div>
                <div class="ibox-content">
                    <div class="flot-chart farm-search">
                        <div class="row-fluid">
                            <div class="col-md-12">
                                <!--<form class="form-inline" role="form" method="get" action="/QualityReports/ByTaster">-->
                                <form role="form" class="filter-form" data-bind="with: QualityReports">
                                    <!--<div class="form-group">
        <label for="year">@Message.Start_Date</label>
        <input type="date" id="start" name="start" class="form-control" value="@ViewBag.SelectedStart" />
    </div>
    <div class="form-group">
        <label for="year">@Message.End_Date</label>
        <input type="date" id="end" name="end" class="form-control" value="@ViewBag.SelectedEnd" />
    </div>
        -->
                                    <hr />
                                    <div class="col-lg-6 col-sm-6">
                                        <div class="form-group">
                                            <label for="searchDepartment" class="hidden-xs">@AdminMessage.Department</label>
                                            <select data-bind="value: department, options: $root.departments, optionsText:'name', optionsValue:'id',optionsCaption: '@string.Format(Message.SelectA, AdminMessage.Department)', event:{change:$root.getMunicipalities}" name="searchDepartment" id="searchDepartment" class="form-control"></select>
                                        </div>

                                        <div class="form-group">
                                            <label for="searchMunicipality" class="hidden-xs">@AdminMessage.Municipality</label>
                                            <select data-bind="value: municipality, options: $root.municipalities, optionsText:'name', optionsValue:'id',optionsCaption: '@string.Format(Message.SelectA, AdminMessage.Municipality)', event:{change: $root.getVillages}" name="searchMunicipality" id="searchMunicipality" class="form-control"></select>
                                        </div>

                                        <div class="form-group">
                                            <label for="searchVillage" class="hidden-xs">@AdminMessage.Village</label>
                                            <select data-bind="value: village, options: $root.villages, optionsText:'name', optionsValue:'id',optionsCaption: '@string.Format(Message.SelectA, AdminMessage.Village)'" name="searchVillage" id="searchVillage" class="form-control"></select>
                                        </div>
                                    </div>
                                    <div class="col-lg-6 col-sm-6">
                                        <div class="form-group">
                                            <label for=" primary-code" class="hidden-xs ">@FarmMessages.Code</label>
                                            <input type="text" placeholder="@FarmMessages.Code" data-bind="value: code" class="form-control" name="searchCode" id="searchCode">
                                        </div>
                                        <div class="form-group">
                                            <label for=" primary-code" class="hidden-xs ">Document</label>
                                            <input type="text" placeholder="Document" data-bind="value: document" class="form-control" name="searchDocument" id="searchDocument">
                                        </div>
                                        <div class="form-group">
                                            <label for=" primary-code" class="hidden-xs ">Farm manager</label>
                                            <input type="text" placeholder="Farm manager" data-bind="value: farmmanager" class="form-control" name="searchFarmManager" id="searchFarmManager">
                                        </div>
                                        <div class="form-group">
                                            <label for=" primary-code" class="hidden-xs ">Name farm</label>
                                            <input type="text" placeholder="NameFarm" data-bind="value: namefarm" class="form-control" name="searchNameFarm" id="searchNameFarm">
                                        </div>
                                    </div>

                                    @if (User.IsQuality())
                                    {
                                        <div class="form-group">
                                            <label for="year">@QualityMessage.Taster</label>
                                            @Html.DropDownList("tasterId", ViewBag.Tasters as System.Web.Mvc.SelectList, string.Format(Message.SelectA, QualityMessage.Taster), new { @class = "form-control" })
                                        </div>
                                    }
                                    else
                                    {
                                        <input type="hidden" name="tasterId" class="form-control" value="@User.UserId" />
                                    }
                                    @*
             <br /><div>

            <label for="typeQuality">Quality</label>
            @Html.RadioButton("Quality", "Todos", true, new { value = "*"})
            @Html.RadioButton("Quality", "OK", new { value = "OK" })
            @Html.RadioButton("Quality", "NO OK", new { value = "NO_OK"})
        </div>*@
                                    <button type="submit" class="btn btn-default">@Message.Search</button>
                                </form>
                            </div>
                        </div>
                        <hr />
                        <div class="row">
                            <div class="col-md-12">
                                <div class="table-responsive horizontal-scroll">
                                    @Html.Partial("_byTasterReport", Model)
                                </div>
                                <br />
                                @*Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount
                                    @Html.PagedListPager(Model, page => Url.Action("ByTaster", new { page, tasterId = ViewBag.SeletedTasterId, start = ViewBag.SelectedStart, end = ViewBag.SelectedEnd }))*@
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<script src="~/Scripts/jquery-ui-1.10.4.min.js"></script>
<link href="~/Content/themes/base/all.css" rel="stylesheet" />
<script>
    $(function () {
        $("#start").datepicker({
            changeMonth: true,
            changeYear: true,
            dateFormat: "yy-mm-dd",
            /*Configuracion para mostrar la fecha abajo del textbox*/
            beforeShow: function (input, inst) {
                var rect = input.getBoundingClientRect();
                setTimeout(function () {
                    inst.dpDiv.css({ top: rect.top + 40, left: rect.left + 0 });
                }, 0);
            }
        });
        $("#end").datepicker({
            changeMonth: true,
            changeYear: true,
            dateFormat: "yy-mm-dd",
            beforeShow: function (input, inst) {
                var rect = input.getBoundingClientRect();
                setTimeout(function () {
                    inst.dpDiv.css({ top: rect.top + 40, left: rect.left + 0 });
                }, 0);
            }
        });
    });
</script>



@* hace referencia a la tabla que esta en el partialView esto le adiciona las opciones de filtrado paginacion y busqueda *@
@section scripts{
    <!--
    <script type="text/javascript" language="javascript" src="//cdn.datatables.net/1.10.8/js/jquery.dataTables.min.js"></script>

    <script type="text/javascript" language="javascript" src="//cdn.datatables.net/plug-ins/be7019ee387/integration/bootstrap/3/dataTables.bootstrap.js"></script>
    --->
    <!-- Script para ordenar columnas // para que el usuario pueda ordenar las columnas -->
    <!--  <script type="text/javascript" language="javascript" src="//cdn.datatables.net/colvis/1.1.0/js/dataTables.colVis.min.js"></script>

      <script type="text/javascript" language="javascript" src="https://cdn.datatables.net/buttons/1.0.3/js/dataTables.buttons.min.js"></script>
      <script type="text/javascript" language="javascript" src="//cdnjs.cloudflare.com/ajax/libs/jszip/2.5.0/jszip.min.js"></script>
      <script type="text/javascript" language="javascript" src="//cdn.rawgit.com/bpampuch/pdfmake/0.1.18/build/pdfmake.min.js"></script>
      <script type="text/javascript" language="javascript" src="//cdn.rawgit.com/bpampuch/pdfmake/0.1.18/build/vfs_fonts.js"></script>
      <script type="text/javascript" language="javascript" src="//cdn.datatables.net/buttons/1.0.3/js/buttons.html5.min.js"></script>
        -->
    <script type="text/javascript">

        $(document).ready(function () {
            //Disable the js alert for the datatable error
            window.alert = function () { };

            var table = $('#report').DataTable({
                buttons: [
                    'excelHtml5'
                ],
                initComplete: function () {
                    this.api().columns().every(function () {
                        if (this[0] != 0) {
                            var column = this;
                            var select = $('<select><option value=""></option></select>')
                                .appendTo($(column.footer()).empty())
                                .on('change', function () {
                                    var val = $.fn.dataTable.util.escapeRegex(
                                        $(this).val()
                                    );

                                    column
                                        .search(val ? '^' + val + '$' : '', true, false)
                                        .draw();
                                });

                            column.data().unique().sort().each(function (d, j) {
                                select.append('<option value="' + d + '">' + d + '</option>')
                            });
                        }

                    });
                }

            });

            table.buttons(0, null).container().prependTo(
                table.table().container()
            );

        });
    </script>
}




