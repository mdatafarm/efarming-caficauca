@model EFarming.Core.TasqModule.SubModule

@{
    ViewBag.Title = "Delete";
}

<div class="remote">
    <div class="col-md-10 col-md-offset-1">
        <div class="alert alert-danger fade in">
            <h4>Eliminar</h4>

            @*<p>
                Are you sure you want to delete this?
            </p>*@
            <p>
                <dl class="dl-horizontal">
                    <dt>
                        Nombre Categoría
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.Module.Name)
                    </dd>

                    <dt>
                        Nombre Sub Categoría
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.Name)
                    </dd>

                    <dt>
                        Orden
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.SubModuleOrder)
                    </dd>
                </dl>
            </p>
            <p>
                @using (Html.BeginForm("Delete", "SubModules", null, FormMethod.Post, new { @class = "to-validate remote-form", role = "form", @data_target = "submodules-content" }))
                {
                    @Html.AntiForgeryToken()
                    
                    <div class="form-group">
                        <center>
                            <label id="information"></label>
                        </center>
                        <br />
                        <div class="col-sm-offset-3 col-sm-9">
                            <button type="submit" class="btn btn-danger" id="SaveFarm">Eliminar</button>
                            <a class="btn btn-link modal-close" id="btnClosePopup" data-dismiss="modal">Cerrar</a>
                        </div>
                    </div>
                }
                <br />
                <br />
            </p>
        </div>
    </div>
</div>
<script src="~/Scripts/remote-validation.js"></script>
